<div class="container">
  <div class="row" style="padding: 50px;">
    <div class="col-md-3"></div>
    <div class="col-md-6" style="text-align: center;">
      <div class="registration-right-box" style="display: inline-block;">
        <div class="registration-form-box">
          <h1>Insta!</h1>
          <div class="registration-form-small-box">

            <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <%= render "devise/shared/error_messages", resource: resource %>

              <%# <div class="registration-form-tiny-box"> %>
                <div class="session-input-part">
                    <div class="field">
                      <%# <%= f.label :email %>
                      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Eメールアドレス" %>
                    </div>
                </div>
              <%# </div> %>

              <%# <div class="registration-form-tiny-box"> %>
                <div class="sessiton-input-part">
                  <div class="field">
                    <%# <%= f.label :パスワード %>
                    <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワード" %>
                  </div>
                </div>

              <%# <div class="registration-submit-box"> %>
                <div class="registration-submit-small-box">
                  <%= f.submit "ログイン", style: "margin-top: 10px;" %>
                </div>
              <%# </div> %>
            <% end %>

            <div style="margin-top: 15px;">
              <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
                <%= link_to "パスワードを忘れたら？", new_password_path(resource_name) %><br />
              <% end %>
            </div>

            <div class="session-or-border-line-box">
              <div class="or-border-line"></div>
              <div class="or-message">または</div>
              <div class="or-border-line"></div>
            </div>

            <div class="registration-facebook-box">
                <%- if devise_mapping.omniauthable? %>
                  <%- resource_class.omniauth_providers.each do |provider| %>
                    <%= link_to omniauth_authorize_path(resource_name, provider) do %><br />
                      <button class="facebook-auth" style="margin-bottom: 15px;">
                        <span style="margin-bottom: 10px;">Facebookでログイン</span>
                      </button>
                    <% end %>
                  <% end %>
                <% end %>
            </div>

          </div>
        </div>

        <div class="registration-login-box">
          <div class="login-text-box">アカウントをお持ちでないですか？<%= link_to "登録する", new_user_registration_path %></div>
        </div>

        <div class="registration-apps-box">
          <p class="apps-download-text">ダウンロード</p>
          <div class="apps-image-box">
            <div class="app-image"></div>
            <div class="app-image"></div>
          </div>
        </div>
      </div>
    </div>
    <div class="col-md-3"></div>
  </div>
</div>