<%# <% provide(:title, @tag.hashname) %>
<%# 参考にしたURLの筆者のコード上必要だったため記述されていた %>
<%# provideメソッドは(:指定した値, 表示したい変数や値)指定した値に変数を代入させる事ができる。 %>
<%# provideメソッドはview内で記述する。
変数を至るところで記述するよりも、変数の変更を反映させる時に一つの変数を変更するだけで済むため便利かも。 %>
    <%# <%= image_tag("hashtag.png") %>
    <%= @tag.hashname %>
        <%# <%= render 'users/follow_form' %>
        <%# <%= render 'shared/usr_info_default' %>
        <%# <%= render 'shared/stats_default' %>
    <% @post_image.each do |post_image| %>
        <%= attachment_image_tag post_image, :post_image, :fill, 300, 300, format: 'jpeg' %>
        <%# # 多分validation掛けてるからfallbackすることはない, fallback: 'no_image.jpg', size: '300x300' %>
        <%= "#{post_image.favorites.count}いいね" %>
        <%= "#{post_image.post_comments.count}件のコメント"%>
        <%# 良いねとコメントの表示をアイコンに変える %>
    <% end %>
        <%# <%= render @post_image %>