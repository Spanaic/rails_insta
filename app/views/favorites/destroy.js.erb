$("#favorite-button-<%= @post_image.id %>").html("<%= j(render partial: 'favorite', locals: {favorites: @favorites, post_images: @post_images,post_image: @post_image}) %>")
<%# #の後になんの値が入っているの？ => idが入っている %>
<%# 中括弧や（）はキチンとルールに沿って記述をする必要があるので、syntax errorの時は要注意 %>
<%# viewのエラー表示も参照することでerror箇所の早期発見につながる %>

<%# アクションメソッドが実行されて、それからjsファイルが読み込まれ、レンダーという形で表示を変えたい箇所を記述したパーシャルを呼び出す。 %>
<%# レンダーで呼び出す時は、アクション内に必要な変数を定義したか確認する。グローバル変数をローカル変数に変更することも漏らさず忘れずに実行する。 %>

<%# locals:はグローバル変数をローカル変数に変更する際の記述方法 %>